{
  "name": "kovalt-roller-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "emu:test": "firebase emulators:exec --only firestore,database --project demo-kovalt \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "vitest": "^5.0.1"
  }
}
